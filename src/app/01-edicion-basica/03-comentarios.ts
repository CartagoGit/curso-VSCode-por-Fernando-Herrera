/*
    Objetivo:
        Comentar un bloque de código

    Tips:
        ⌘ /
        Ctrl + /
*/

const a = 10;
const b = 20;
const c = { a, b };

const a2 = 30;
const b2 = 40;
const c2 = { a2, b2 };

console.log(a2, b2, c2);

// Resultado final
// Sólo dejar un bloque comentado
